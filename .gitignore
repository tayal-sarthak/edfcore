node_modules/
dist/
.astro/
.env*
.DS_Store
tests/corpus/files/

# Preserved, never committed: see CLAUDE.md file-deletion policy.
deleted/

# Throwaway reproductions written by review agents. Never committed: they assert whatever
# behaviour was current when they were written, so committing them pins defects.
tests/scratch/

# The virtualenv `scripts/golden/README.md` tells you to build for pyedflib and mne. It is created
# at the repository root by following those instructions, and `scripts/release.mjs` stages with
# `git add -A` — so without this line a regeneration run puts a few hundred megabytes of Python
# into a release commit.
.venv/

# Local editor/agent tooling
.claude/
